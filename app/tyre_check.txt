  1 'import sqlite3, logging\n'  2 'from typing import Optional\n'  3 'from fastapi import APIRouter, Query\n'  4 '_DB_PATH = "D:/Vento_Timing/tyre_raw.db"\n'  5 '\n'  6 'logger = logging.getLogger(__name__)\n'  7 'router = APIRouter(prefix="/api/tyre", tags=["tyre_analysis"])\n'  8 '\n'  9 '\n' 10 'class DegradationService:\n' 11 '    def __init__(self, db_path: str = ""):\n' 12 '        self.db_path = db_path or _DB_PATH\n' 13 '\n' 14 '    def _get_conn(self) -> sqlite3.Connection:\n' 15 '        conn = sqlite3.connect(self.db_path)\n' 16 '        conn.row_factory = sqlite3.Row\n' 17 '        return conn\n' 18 '\n' 19 '    def _resolve_session_id(self, session_id: Optional[int] = None) -> Optional[int]:\n' 20 '        """褰?session_id 鏈\ue045寚瀹氭椂锛岃嚜鍔ㄥ彇鏈€鍚庝竴涓?Race 绫诲瀷鐨?session"""\n' 21 '        if session_id is not None:\n' 22 '            return session_id\n' 23 '        conn = self._get_conn()\n' 24 '        row = conn.execute(\n' 25 '            "SELECT id FROM sessions WHERE session_type = \'Race\' ORDER BY id DESC LIMIT 1"\n' 26 '        ).fetchone()\n' 27 '        conn.close()\n' 28 '        return row[0] if row else None\n' 29 '\n' 30 '    def get_available_sessions(self) -> list[dict]:\n' 31 '        conn = self._get_conn()\n' 32 '        rows = conn.execute(\n' 33 '            "SELECT id, session_key, session_name, session_type FROM sessions ORDER BY session_key"\n' 34 '        ).fetchall()\n' 35 '        conn.close()\n' 36 '        return [dict(r) for r in rows]\n' 37 '\n' 38 '    def get_available_drivers(self) -> list[dict]:\n' 39 '        conn = self._get_conn()\n' 40 '        rows = conn.execute(\n' 41 '            "SELECT driver_number, driver_name, team_name FROM drivers ORDER BY driver_number"\n' 42 '        ).fetchall()\n' 43 '        conn.close()\n' 44 '        return [dict(r) for r in rows]\n' 45 '\n' 46 '    def get_laps_for_driver(self, driver_number: int, session_id: int = None,\n' 47 '                             compound: Optional[str] = None,\n' 48 '                             stint_number: Optional[int] = None) -> list[dict]:\n' 49 '        conn = self._get_conn()\n' 50 '        query = """\n' 51 '            SELECT l.*, s.compound, s.stint_number, s.tyre_age_at_start\n' 52 '            FROM laps l JOIN stints s ON l.stint_id = s.id\n' 53 '            WHERE l.driver_number = ?\n' 54 '              AND l.is_outlap = 0 AND l.is_inlap = 0\n' 55 "              AND l.track_status = 'Green' AND l.lap_time IS NOT NULL AND l.lap_time > 0\n" 56 '        """\n' 57 '        params = [driver_number]\n' 58 '        if session_id is not None:\n' 59 '            query += " AND l.session_id = ?"\n' 60 '            params.append(session_id)\n' 61 '        if compound:\n' 62 '            query += " AND s.compound = ?"; params.append(compound)\n' 63 '        if stint_number and stint_number > 0:\n' 64 '            query += " AND s.stint_number = ?"; params.append(stint_number)\n' 65 '        query += " ORDER BY l.lap_number"\n' 66 '        rows = conn.execute(query, params).fetchall()\n' 67 '        conn.close()\n' 68 '        seen = set()\n' 69 '        result = []\n' 70 '        for r in rows:\n' 71 '            if r["lap_number"] not in seen:\n' 72 '                seen.add(r["lap_number"])\n' 73 '                result.append(dict(r))\n' 74 '        return result\n' 75 '\n' 76 '    def compute_degradation(self, laps: list[dict],\n' 77 '                            fuel_correction: bool = False) -> dict:\n' 78 '        """Per-compound degradation calculation."""\n' 79 '        if not laps:\n' 80 '            return {"per_compound": {}}\n' 81 '\n' 82 '        # 鎸?compound 鑱氬悎\n' 83 '        compound_groups = {}\n' 84 '        for lap in laps:\n' 85 '            compound_groups.setdefault(lap["compound"], []).append(lap)\n' 86 '\n' 87 '        per_compound = {}\n' 88 '        for comp, comp_laps in compound_groups.items():\n' 89 '            res = self._process_laps(comp_laps,\n' 90 '                                     fuel_correction=fuel_correction,\n' 91 '                                     is_aggregate=True)\n' 92 '            if res:\n' 93 '                print(f"[DEG] {comp}: {len(res.get(\'all_points\',[]))} all_pts, model={res.get(\'model\',{}).get(\'type\',\'none\')}")\n' 94 '                per_compound[comp] = res\n' 95 '\n' 96 '        print(f"[DEG] total: {len(compound_groups)} compounds, {sum(len(v) for v in compound_groups.values())} laps")\n' 97 '        return {"per_compound": per_compound}\n' 98 '\n' 99 '    def _process_laps(self, laps: list[dict],\n'100 '                      fuel_correction: bool = True,\n'101 '                      is_aggregate: bool = False) -> dict:\n'102 '        if not laps:\n'103 '            return None\n'104 '\n'105 '        # 鎺掑簭\n'106 '        if is_aggregate:\n'107 '            laps.sort(key=lambda x: x["lap_number"])\n'108 '        else:\n'109 '            laps.sort(key=lambda x: x["lap_in_stint"])\n'110 '\n'111 '        ages = []\n'112 '        raw_times = []\n'113 '        for lap in laps:\n'114 '            age_start = lap.get("tyre_age_at_start") or 0\n'115 '            if is_aggregate:\n'116 '                age = lap.get("tyre_age", 0)\n'117 '            else:\n'118 '                age = age_start + lap["lap_in_stint"] - 1\n'119 '            ages.append(age)\n'120 '            raw_times.append(lap["lap_time"])\n'121 '\n'122 '        # 鐕冩补淇\ue1bd\ue11c (婊℃补绛夋晥)\n'123 '        corrected = list(raw_times)\n'124 '        if fuel_correction:\n'125 '            for i, lap in enumerate(laps):\n'126 '                fuel_used = (lap["lap_number"] - 1) * 1.8\n'127 '                corrected[i] = raw_times[i] + fuel_used * 0.03\n'128 '\n'129 '        # 鈹€鈹€ 鑱氬悎妯″紡锛氬\ue63f濂楄儙鏁版嵁锛屾瘡濂?stint 鍚勮嚜褰掍竴鍖?鈹€鈹€\n'130 '        if is_aggregate:\n'131 '            # 鍏ㄥ眬鍩哄噯 = 鎵€鏈夊\ue69c鑳庝慨姝ｅ湀閫熺殑鏈€灏忓€?            global_baseline = min(corrected) if corrected else None\n'132 '            if global_baseline is None:\n'133 '                return {"points": [], "all_points": [], "model": {"type": "none", "params": {}, "curve": []}}\n'134 '\n'135 '            stint_groups: dict[int, list[int]] = {}\n'136 '            for i, lap in enumerate(laps):\n'137 '                sn = lap.get("stint_number") or 0\n'138 '                stint_groups.setdefault(sn, []).append(i)\n'139 '\n'140 '            fit_ages: list[float] = []\n'141 '            fit_losses: list[float] = []\n'142 '            all_points_list: list[dict] = []\n'143 '\n'144 '            for sn, indices in stint_groups.items():\n'145 '                if len(indices) < 2:\n'146 '                    continue\n'147 '                stint_corrected = [corrected[i] for i in indices]\n'148 '                stint_ages = [ages[i] for i in indices]\n'149 '                stint_lap_nums = [laps[i]["lap_number"] for i in indices]\n'150 '\n'151 '                stint_baseline = min(stint_corrected)\n'152 '                stint_losses_rel = [t - stint_baseline for t in stint_corrected]\n'153 '                offset = stint_baseline - global_baseline\n'154 '\n'155 '                mono_ages, mono_losses_rel = [], []\n'156 '                max_loss = -999.0\n'157 '                for a, l in zip(stint_ages, stint_losses_rel):\n'158 '                    if l >= max_loss:\n'159 '                        mono_ages.append(a)\n'160 '                        mono_losses_rel.append(l)\n'161 '                        max_loss = l\n'162 '                if len(mono_ages) < 2:\n'163 '                    continue\n'164 '                mono_losses = [l + offset for l in mono_losses_rel]\n'165 '\n'166 '                for j in range(len(mono_ages)):\n'167 '                    all_points_list.append({\n'168 '                        "tyre_age": mono_ages[j],\n'169 '                        "loss": round(mono_losses[j], 4),\n'170 '                        "lap_number": stint_lap_nums[j] if j < len(stint_lap_nums) else 0,\n'171 '                    })\n'172 '                fit_ages.extend(mono_ages)\n'173 '                fit_losses.extend(mono_losses)\n'174 '\n'175 '            if not fit_ages:\n'176 '                return {"points": [], "all_points": [], "model": {"type": "none", "params": {}, "curve": []}}\n'177 '\n'178 '            # 绗?灞傦細鎸夊勾榫勫垎妗跺彇涓\ue15d綅鏁帮紙娑堥櫎澶氬\ue69c鑳庡悓骞撮緞涓嶅悓鍩哄噯鐨勫樊寮傦級\n'179 '            age_map: dict[int, list[float]] = {}\n'180 '            for a, l in zip(fit_ages, fit_losses):\n'181 '                key = int(round(a))\n'182 '                age_map.setdefault(key, []).append(l)\n'183 '\n'184 '            aligned_ages, aligned_losses = [], []\n'185 '            for key in sorted(age_map.keys()):\n'186 '                vals = sorted(age_map[key])\n'187 '                aligned_ages.append(float(key))\n'188 '                aligned_losses.append(vals[len(vals) // 2])\n'189 '\n'190 '            # 绗?灞傦細寮哄埗鍗曡皟閫掑\ue583锛堝悗涓€鍦堟崯澶变笉鑳藉皬浜庡墠涓€鍦堬級\n'191 '            for i in range(1, len(aligned_losses)):\n'192 '                if aligned_losses[i] < aligned_losses[i - 1]:\n'193 '                    aligned_losses[i] = aligned_losses[i - 1]\n'194 '\n'195 '            # 瀵归綈鍚庣殑鏁版嵁鐢ㄤ簬鎷熷悎锛屽師濮嬬偣鐢ㄤ簬灞曠ず\n'196 '            fit_ages, fit_losses = aligned_ages, aligned_losses\n'197 '            all_points = all_points_list\n'198 '\n'199 '        else:\n'200 '            # 鍗?stint锛氬叏灞€鍩哄噯锛堜笉璧板垎灞傛竻娲楋紝鐩存帴鍙?fastest锛?            baseline = min(corrected) if corrected else None\n'201 '            if baseline is None:\n'202 '                return None\n'203 '            losses = [t - baseline for t in corrected]\n'204 '            all_points = [\n'205 '                {"tyre_age": ages[i], "loss": round(l, 4), "lap_number": laps[i].get("lap_number", 0)}\n'206 '                for i, l in enumerate(losses)\n'207 '            ]\n'208 '            fit_ages = list(ages)\n'209 '            fit_losses = list(losses)\n'210 '\n'211 '        # 寮傚父鍊艰繃婊わ細鎵€鏈夋ā寮忓潎浣跨敤纭\ue104槇鍊艰寖鍥达紙loss > 5.0s 鎴?< -0.8s 涓哄紓甯革級\n'212 '        if not fit_ages:\n'213 '            return {"points": [], "all_points": all_points, "model": {"type": "none", "params": {}, "curve": []}}\n'214 '\n'215 '        valid_indices = [i for i in range(len(fit_losses)) if -0.3 <= fit_losses[i] < 5.0]\n'216 '\n'217 '        if len(valid_indices) < 2:\n'218 '            return {"points": [], "all_points": all_points, "model": {"type": "none", "params": {}, "curve": []}}\n'219 '\n'220 '        valid_ages = [fit_ages[i] for i in valid_indices]\n'221 '        valid_losses = [fit_losses[i] for i in valid_indices]\n'222 '        from .degradation_model import fit_best_model, model_to_dict\n'223 '\n'224 '        # 鐐瑰皯鏃跺洖閫€绾挎€?\n'225 '        if len(valid_ages) < 3:\n'226 '            if len(valid_ages) == 2:\n'227 '                slope = (valid_losses[1] - valid_losses[0]) / max(valid_ages[1] - valid_ages[0], 1e-10)\n'228 '                icpt = valid_losses[0] - slope * valid_ages[0]\n'229 '                a_min, a_max = min(valid_ages), max(valid_ages)\n'230 '                model_dict = {"type": "linear",\n'231 '                    "params": {"slope": round(slope,6), "intercept": round(icpt,6)},\n'232 '                    "r_squared": 1.0,\n'233 '                    "curve": [{"age": round(a,1), "degradation": round(slope*a+icpt,4)}\n'234 '                              for a in (a_min, a_max)]}\n'235 '            else:\n'236 '                model_dict = {"type": "none", "params": {}, "curve": []}\n'237 '        else:\n'238 '            model = fit_best_model(valid_ages, valid_losses, clean=True)\n'239 '            model_dict = model_to_dict(model, age_min=int(min(valid_ages)),\n'240 '                                      age_max=int(max(valid_ages)))\n'241 '\n'242 '        points = [{"tyre_age": a, "loss": round(l,4), "lap_number": 0}\n'243 '                  for a,l in zip(valid_ages, valid_losses)]\n'244 '        return {"points": points, "all_points": all_points, "model": model_dict}\n'245 '\n'246 '    def get_qualy_best_lap(self, driver_number, compound):\n'247 '        conn = self._get_conn()\n'248 '        where = "l.track_status = \'Green\' AND l.is_outlap = 0 AND l.is_inlap = 0 AND l.lap_time > 0"\n'249 '        base = "SELECT MIN(l.lap_time) FROM laps l"\n'250 '        base += " JOIN stints st ON l.stint_id = st.id"\n'251 '        base += " JOIN sessions s ON l.session_id = s.id"\n'252 '        base += f" WHERE l.driver_number = ? AND st.compound = ? AND {where}"\n'253 '\n'254 '        # 浼樺厛鎺掍綅\n'255 '        row = conn.execute(base + " AND s.session_type = \'Qualifying\'", (driver_number, compound)).fetchone()\n'256 '        best = row[0] if row and row[0] else None\n'257 '        source = "Qualifying" if best else None\n'258 '\n'259 '        # 鏃犳帓浣嶆暟鎹\ue1bc垯鍥為€€鍒扮粌涔犺禌\n'260 '        if best is None:\n'261 '            row = conn.execute(base + " AND s.session_type = \'Practice\'", (driver_number, compound)).fetchone()\n'262 '            best = row[0] if row and row[0] else None\n'263 '            source = "Practice" if best else None\n'264 '\n'265 '        conn.close()\n'266 '        return {"best_lap": round(best, 3) if best else None, "source": source}\n'267 '\n'268 '\n'269 '@router.get("/qualy-best")\n'270 'def api_qualy_best(driver: int = Query(description="Driver"), compound: str = Query(description="Compound")):\n'271 '    return get_service().get_qualy_best_lap(driver, compound)\n'272 '\n'273 '\n'274 '_service: DegradationService = None\n'275 '\n'276 '\n'277 'def get_service() -> DegradationService:\n'278 '    global _service\n'279 '    if _service is None:\n'280 '        _service = DegradationService()\n'281 '    return _service\n'282 '\n'283 '\n'284 '@router.get("/drivers")\n'285 'def api_get_drivers():\n'286 '    svc = get_service()\n'287 '    try:\n'288 '        return {"drivers": svc.get_available_drivers(), "sessions": svc.get_available_sessions()}\n'289 '    except Exception as e:\n'290 '        return {"error": str(e)}\n'291 '\n'292 '\n'293 '@router.get("/degradation")\n'294 'def api_get_degradation(\n'295 '    driver: int = Query(description="Driver number"),\n'296 '    session: Optional[int] = Query(None, description="Session ID"),\n'297 '    compound: Optional[str] = Query(None),\n'298 '    stint: Optional[int] = Query(None),\n'299 '):\n'300 '    svc = get_service()\n'301 '    resolved_session = svc._resolve_session_id(session)\n'302 '    laps = svc.get_laps_for_driver(driver, resolved_session, compound, stint)\n'303 '    result = svc.compute_degradation(laps)\n'304 '    return {"driver": driver, "session_id": resolved_session, "data": result}\n'305 '\n'306 '\n'307 '@router.get("/compare")\n'308 'def api_compare_degradation(\n'309 '    drivers: str = Query(description="Comma-separated driver numbers, e.g. 1,44"),\n'310 '    session: Optional[int] = Query(None),\n'311 '    compound: Optional[str] = Query(None),\n'312 '    stint: Optional[int] = Query(None),\n'313 '):\n'314 '    svc = get_service()\n'315 '    resolved_session = svc._resolve_session_id(session)\n'316 '    try:\n'317 '        driver_list = [int(d.strip()) for d in drivers.split(",")]\n'318 '    except (ValueError, TypeError):\n'319 '        return {"error": "Invalid driver list"}\n'320 '    result = {}\n'321 '    for dn in driver_list:\n'322 '        laps = svc.get_laps_for_driver(dn, resolved_session, compound, stint)\n'323 '        result[str(dn)] = svc.compute_degradation(laps)\n'324 '    return {"session_id": resolved_session, "drivers": result}\n'